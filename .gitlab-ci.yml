stages:
  - build

build_app:
  stage: build
  image: node:latest
  script:
    - yarn install
    - react-scripts build
    - yarn install -g electron-builder
    - yarn dist:win
  artifacts:
    paths:
      - dist/
  only:
    - main